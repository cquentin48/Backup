import React from "react"

export default class TopBar extends React.Component {
    render (): React.ReactNode {
        return (
            <div>
                Top bar here!
            </div>
        )
    }
}
