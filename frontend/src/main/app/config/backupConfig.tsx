/**
 * Website configuration namespace
 */
export const Config = {
    /**
     * GraphQL backend URL in docker compose (located in the ``backend`` container)
     */
    GQL_BACKENDURI: 'http://localhost/api/v1/data/graphql/'
}
