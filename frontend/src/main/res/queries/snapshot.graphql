query getSaveInfos($snapshotID:String!){
  snapshotInfos(snapshotId: $snapshotID) {
    versions{
      chosenVersion
      name
      installType
    }
    repositories{
      sourcesLines
      name
    }
    operatingSystem
  }
}