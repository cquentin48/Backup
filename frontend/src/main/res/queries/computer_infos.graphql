query getDeviceInfos($deviceID:String!){
  deviceInfos(deviceId:$deviceID){
    cores,
    memory,
    name,
    operatingSystem,
    processor,
    snapshots{
      snapshotId,
      snapshotDate
    }
  }
}